# The two checkouts, by absolute path. Same commit; the harness never changes
# these per ticket.
CONTROL_REPO=/absolute/path/to/control-checkout
GREPTILE_REPO=/absolute/path/to/greptile-checkout

# Use danger-full-access only inside the disposable Linux VMs described by the
# experiment. Local development defaults to workspace-write when this is unset.
# CODEX_SANDBOX=danger-full-access

# Where to find Codex sessions, for copying transcripts. Defaults to ~/.codex.
# CODEX_HOME=/absolute/path/to/.codex

# Everything else is fixed in code, not configured here: results land in
# ./results, arms get 3 attempts, the idle watchdog is 10m, and Greg Tile
# (bun run greg) adds no configuration of its own — it reuses the arms above
# and, if a Linear MCP is configured in your Codex environment, files a ticket
# per rung.
