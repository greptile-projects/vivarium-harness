{
  "name": "vivarium-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "mirror-snapshot": "bun src/mirror/snapshot.ts",
    "start": "bun src/index.ts",
    "test": "bun test test/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ink": "^5",
    "react": "^18"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18",
    "typescript": "^7.0.2"
  }
}
